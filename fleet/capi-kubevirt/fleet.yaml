defaultNamespace: capkv-system
dependsOn:
  - name: clusters-hcp-demo-fleet-kubevirt-operator
diff:
  comparePatches:
    - apiVersion: admissionregistration.k8s.io/v1
      kind: ValidatingWebhookConfiguration
      operations:
        - op: remove
          path: /webhooks
